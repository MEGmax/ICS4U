// Drawing Points

size(600, 200);
// This line creates an output window with a size
// of 600 pixels wide by 200 pixels high

smooth();
// Turn anti-aliasing on

background(#666666);
// colour background in hexadecimal format

//Points
stroke(#607F9C);
// This line gives the point its colour. The value is
// stated in hexadecimal format.

strokeWeight(20);
// This line specifies the stroke weight(thickness)
// of 20 pixels 

// The standard format of a point is
// point(x,y);
// The first argument is the x coordinate and the second argument
// corresponds to the y coordinate
 
point(100, 100);
// This first point is located 100 pixels over from the left
// part of the screen and 100 pixels down.

point(150, 100);
point(200, 100);
point(250, 100);
point(300, 100);
point(350, 100);
point(400, 100);
point(450, 100);
point(500, 100);
